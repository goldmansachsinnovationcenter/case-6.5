import * as cdk from '@aws-cdk/core';

export class MyCustomConstruct extends cdk.Construct {
  constructor(scope: cdk.Construct, id: string) {
    super(scope, id);

    // Define your custom resources here
  }
}